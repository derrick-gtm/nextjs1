# NEXUS OS — UI Prototype

Fully interactive UI prototype. No backend, no env vars required.

## Run locally

```bash
npm install
npm run dev
```

## Pages

- `/hub` — Personal Hub
- `/org` — Organization dashboard
- `/members` — Member directory
- `/events` — Events + detail
- `/calendar` — Month/week/list calendar
- `/discover` — Public event discovery
- `/notifications` — Notification center
- `/settings` — User settings
